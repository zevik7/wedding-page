import { Ephesis, Merienda } from 'next/font/google';

export const merienda = Merienda({
  subsets: ['vietnamese'],
  weight: ['300', '400', '500', '600'],
});

export const ephesis = Ephesis({
  subsets: ['vietnamese'],
  weight: ['400'],
});
